# Ogorodnik Adam 4cs-31

1. Clone the repository:
   git clone https://github.com/AdamT3H/ihtLab3.git
   cd ihtLab2
   
Grant execute permissions:
2. chmod +x run.sh user-data.sh

Run the main script:
3. ./run.sh

The script will automatically call user-data.sh, which will perform system configuration tasks.

After entering aws machin, open sysinfo
4. tail -n 30 /var/log/sysinfo

